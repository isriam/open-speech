# Voice presets — custom named voices for easy selection
# Set TTS_VOICES_CONFIG=voice-presets.yml to use
presets:
  will:
    voice: "am_puck(1)+am_liam(1)+am_onyx(0.5)"
    speed: 1.2
    description: "Will's voice — blended male (puck + liam + onyx)"

  female:
    voice: "af_jessica(1)+af_heart(1)"
    speed: 1.2
    description: "Female voice — blended (jessica + heart)"

  british-butler:
    voice: "bm_george"
    speed: 0.9
    description: "Distinguished British male"
